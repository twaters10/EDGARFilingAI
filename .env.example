# Copy to .env and fill in. .env is git-ignored; this file is committed.
# This repository is PUBLIC — never put a real secret in .env.example.

# --- EDGAR (required) ---------------------------------------------------------
# SEC requires a declared User-Agent containing a real contact address.
# https://www.sec.gov/os/webmaster-faq#developers
# The application refuses to start while this is still the placeholder below.
EDGAR_USER_AGENT="Your Name your.email@example.com"

# Requests per second. SEC permits 10; we default to 5 for headroom (ADR-0015).
EDGAR_RATE_LIMIT=5.0
EDGAR_MAX_RETRIES=5
EDGAR_TIMEOUT_SECONDS=30.0

# --- Storage ------------------------------------------------------------------
DATA_DIR=data

# --- AWS (unused until Stage 8; declared so nothing is ever hardcoded) --------
# AWS_REGION=us-east-1
# S3_BUCKET=
